# No mutations currently used
mutation createUser($user: CreateUserInput!) {
    createUser(user: $user) {
        code
        message
        translationCode
        user {
            id
            name
            surname
            email
            phone
            role
            username
            active
            deviceVisibility
            vodafoneCountryVisibility
            creationDate
            updateDate
        }
    }
}

mutation updateUser($userInfo: UpdateUserInput!) {
  updateUser(userInfo: $userInfo) {
    code
    message
    translationCode
    user {
      id
      name
      surname
      email
      phone
      role
      username
      active
      deviceVisibility
      vodafoneCountryVisibility
      creationDate
      updateDate
    }
  }
}

mutation deleteUser($id: String!) {
  deleteUser(id: $id) {
    code
    message
    translationCode
  }
}