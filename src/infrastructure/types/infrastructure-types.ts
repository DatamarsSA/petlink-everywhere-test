export const HTTP_HEADERS = {
  AUTHORIZATION: "Authorization",
  API_KEY: "x-api-key",
};

export enum ServiceType {
  CORE = "CORE",
  CCT = "CCT",
}

export enum AuthMode {
  TOKEN = "token",
  API_KEY = "apiKey",
}
